local options = {
  ensure_installed = { "lua","cpp" ,"regex","c","python","yaml"},

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
